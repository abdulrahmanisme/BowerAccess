[build]
command = "bun run build"
publish = "dist"

[build.environment]
SECRETS_SCAN_OMIT_KEYS = "VITE_POSTHOG_HOST,VITE_SUPABASE_PROJECT_ID"

# SPA fallback for client-side routes
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
